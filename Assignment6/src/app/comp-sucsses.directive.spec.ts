import { CompSucssesDirective } from './comp-sucsses.directive';

describe('CompSucssesDirective', () => {
  it('should create an instance', () => {
    const directive = new CompSucssesDirective();
    expect(directive).toBeTruthy();
  });
});
